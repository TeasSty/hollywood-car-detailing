import { defineConfig } from "astro/config";

export default defineConfig({
  site: "https://teassty.github.io",
  base: "/hollywood-car-detailing",
  output: "static",
  trailingSlash: "always",
});
